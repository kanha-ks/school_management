<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!--    this is the view that will display on the screen-->
    <record id="school_management_subject_form_view" model="ir.ui.view">
        <field name="name">school.subject.view.form</field>
        <field name="model">school.subject</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="class_name" widget="many2many_tags"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!--    this is the list view-->
    <record id="school_management_subject_list_view" model="ir.ui.view">
        <field name="name">school.subject.view.list</field>
        <field name="model">school.subject</field>
        <field name="arch" type="xml">
            <list string="Your Subjects List">
                <field name="name"/>
                <field name="class_name" widget="many2many_tags"/>
            </list>
        </field>

    </record>
    <!--    this is the action-->
    <record id="action_school_management_subject" model="ir.actions.act_window">
        <field name="name">Subject</field>
        <field name="res_model">school.subject</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>